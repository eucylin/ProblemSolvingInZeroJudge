#include <iostream>
#include <string>
using namespace std;

int main(){
	int T, N, x1, x2, y1, y2, h1, h2, top;
	cin >> T;
	while (T--){
		cin >> N;
		while (N--){
			cin >> x1 >> x2 >> y1 >> y2 >> h1 >> h2;
		}
	}
	return 0;
}